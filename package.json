{
  "name": "vulcaningress",
  "version": "0.0.14",
  "description": "Service that polls a kubernetes API and gets a list of labelled services then adds them as backends in hipache via redis",
  "main": "app.js",
  "dependencies": {
    "http": "0.0.0",
    "nodejs-etcd": "^0.1.1",
    "redis": "^2.5.3",
    "repeat": "0.0.6",
    "request": "^2.71.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT"
}
